
# This policy repersents a safe
# loaded on 'root' or '{{ LOB }}' subbranch
---
- !policy
  id: safeName
  annotations:
    cyberark: safe

  body:
  # [read, execute]
  - !group read-only
  # [update]
  - !group write-only
  # [read]
  - !group auditor
  # [read, execute, update]
  - !group read-write


